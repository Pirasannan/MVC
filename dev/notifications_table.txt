-- Notifications table for storing sent notifications
CREATE TABLE notifications (
    id INT AUTO_INCREMENT PRIMARY KEY,
    recipient_type ENUM('admin', 'doctor', 'patient', 'all') NOT NULL,
    recipient_id INT NULL, -- NULL for 'all' notifications
    title VARCHAR(255) NOT NULL,
    message TEXT NOT NULL,
    notification_type ENUM('info', 'warning', 'success', 'error') DEFAULT 'info',
    status ENUM('sent', 'read', 'unread') DEFAULT 'sent',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    read_at DATETIME NULL,
    INDEX idx_recipient (recipient_type, recipient_id),
    INDEX idx_status (status),
    INDEX idx_created_at (created_at)
);

-- Sample data for testing
INSERT INTO notifications (recipient_type, recipient_id, title, message, notification_type, status) VALUES
('all', NULL, 'System Maintenance', 'Scheduled maintenance will occur tonight from 2-4 AM', 'info', 'sent'),
('doctor', 1, 'New Patient Registration', 'A new patient has registered and requires verification', 'info', 'sent'),
('patient', 2, 'Appointment Reminder', 'Your appointment is scheduled for tomorrow at 2 PM', 'info', 'sent'),
('admin', 1, 'Doctor Verification Required', '3 new doctors are pending verification', 'warning', 'sent');
