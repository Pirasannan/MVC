# Verification uploads directory
Options -Indexes
# Allow image files only
<FilesMatch "\.(jpg|jpeg|png)$">
    Allow from all
</FilesMatch>
# Deny everything else
<FilesMatch "^(?!.*\.(jpg|jpeg|png)$).*$">
    Deny from all
</FilesMatch>
